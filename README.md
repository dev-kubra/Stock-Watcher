# Stock-Watcher
Stock Watcher is a lightweight application that automatically tracks product availability by size or variant. It maps variants to unique identifiers, checks stock status at regular intervals, and notifies the user when the selected option becomes available.
